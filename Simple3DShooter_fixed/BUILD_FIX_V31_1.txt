BUILD FIX V31.1

Исправлена блокирующая ошибка компиляции main.cpp:
- default argument для параметра spacing у drawText задавался дважды: в предварительном объявлении и в определении.
- default argument оставлен только в предварительном объявлении; определение drawText теперь принимает spacing без значения по умолчанию.

Дополнительно:
- предупреждение о неиспользуемом параметре spacing в drawTextImpact устранено через явное подавление unused parameter.
- CMakeLists.txt не менялся по логике сборки V31.

Проверка:
- CMake успешно разбирает CMakeLists.txt и доходит до поиска OpenGL.
- Полная сборка в текущем Linux-окружении невозможна из-за отсутствия системных OpenGL библиотек.
- На Windows/MSYS2 UCRT64 проект должен собираться дальше ошибки default argument.
